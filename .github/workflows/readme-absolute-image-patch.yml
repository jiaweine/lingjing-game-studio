name: Apply README Absolute Image Fix

on:
  push:
    branches: [agent/diagnose-readme-browser-images]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: agent/diagnose-readme-browser-images
      - name: Replace README image sources
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('README.md')
          text = path.read_text(encoding='utf-8')
          names = [
              'auth', 'workspace-empty', 'upload', 'task-running',
              'workspace', 'evidence', 'multimodal', 'cover',
          ]
          for name in names:
              old = f'./docs/assets/readme/{name}.png'
              new = f'https://raw.githubusercontent.com/jiaweine/lingjing-game-studio/main/docs/assets/readme/{name}.png'
              text = text.replace(old, new)
          path.write_text(text, encoding='utf-8')
          PY
          git diff -- README.md
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add README.md
          if git diff --cached --quiet; then
            echo 'README already uses absolute raw image URLs.'
            exit 0
          fi
          git commit -m 'docs: bypass broken GitHub raw route for README images'
          git push origin HEAD:agent/diagnose-readme-browser-images
